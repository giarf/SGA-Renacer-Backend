meta {
  name: Get Entidades
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/entidades
  body: none
  auth: none
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });

  test("Response is array", function() {
    expect(res.body).to.be.an("array");
  });
}
