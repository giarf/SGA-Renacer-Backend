meta {
  name: Delete Persona
  type: http
  seq: 99
}

delete {
  url: {{baseUrl}}/api/personas/{{personaId}}
  body: none
  auth: none
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });

  test("Delete success message", function() {
    expect(res.body.mensaje).to.include("eliminada exitosamente");
  });
}
